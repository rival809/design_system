{
  "version": 2,
  "framework": null,
  "installCommand": "if [ ! -d \"$HOME/flutter\" ]; then git clone --depth 1 https://github.com/flutter/flutter.git -b stable $HOME/flutter; fi && export PATH=\"$HOME/flutter/bin:$PATH\" && flutter --version && cd example && flutter pub get",
  "buildCommand": "export PATH=\"$HOME/flutter/bin:$PATH\" && cd example && flutter build web --release",
  "outputDirectory": "example/build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}