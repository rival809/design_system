{
  "version": 2,
  "framework": null,
  "installCommand": "if [ ! -d \"$HOME/flutter\" ]; then git clone --depth 1 https://github.com/flutter/flutter.git -b stable $HOME/flutter; fi && export PATH=\"$HOME/flutter/bin:$PATH\" && flutter --version && flutter pub get",
  "buildCommand": "export PATH=\"$HOME/flutter/bin:$PATH\" && flutter build web --release",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}